name: Deploy portfolio to GitHub Pages

on:
  push:
    branches: [main]
  # 記事・スライド・コントリビューションを毎朝取り直して公開する（JST 6:00）
  schedule:
    - cron: '0 21 * * *'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Marp のデッキは Chrome で PDF 化する。変わっていないデッキは
      # scripts/decks/cache.mjs のフィンガープリントで再生成を飛ばせるので、
      # その成果物とキャッシュを持ち越す。
      - name: Restore built decks
        uses: actions/cache@v6
        with:
          path: |
            .cache
            public/decks
            public/pdfjs
          # 出力を作るコードも鍵に混ぜる。パイプラインは scripts/decks/ に
          # 分かれていて build-decks.mjs は組み立てだけなので、そちらを外すと
          # 「レンダリングを直したのに CI では前の絵が復元される」が起きる。
          # OUTPUT_CONFIG.version を上げ忘れても、ここで空振りして作り直せる
          key: decks-${{ hashFiles('decks/**', 'scripts/build-decks.mjs', 'scripts/decks/**') }}
          restore-keys: decks-

      - name: Fetch content and build
        env:
          # contributions は GraphQL のため認証が必要。GH_PAT (public_repo / read:user)
          # が設定されていればそれを使い、無ければ GITHUB_TOKEN で試す。
          # 取得に失敗してもコミット済みの src/data/generated.json でビルドは通る。
          GITHUB_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          # ユーザーサイト (<user>.github.io) なのでルート配信
          BASE_PATH: /
        run: npm run build

      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
